import Main from "@/components/Main";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "TV Playlists ⋅ Dashboard",
   
  };

export default function Home() {
    
    const isAuthenticated = false; 
    return (
      <Main>
    
      </Main>
    );
  }